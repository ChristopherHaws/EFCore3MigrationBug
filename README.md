# EF Core 3 Migration Bug

### Steps to reproduce
1) Open a command prompt to the `EFCore3MigrationBug` directory
2) Run the following command:

```powershell
dotnet ef migrations add EFCore30Migration
```